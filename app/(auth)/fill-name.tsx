import React, { FC } from "react";
import UserName from "../../features/settings/UserName";

const FillNameForm: FC = () => {
  return <UserName isInitial />;
};

export default FillNameForm;
